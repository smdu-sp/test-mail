import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["localhost", "127.0.0.1", "eleicaocmpu2025.prefeitura.sp.gov.br"],
  /* config options here */
};

export default nextConfig;
